classdef Cache < handle
    %Cache class used to cache small variables through the execution 
    % Used for the handle
    properties
        list (1,1) struct % struct with all variables
    end
end